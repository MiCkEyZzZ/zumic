# Используемое издание Rust
edition = "2021"

# Максимальная ширина строки
max_width = 100

# Табуляция через пробелы (4 пробела)
hard_tabs = false
tab_spaces = 4

# Не объединять короткие цепочки вызовов в одну строку
chain_width = 60
